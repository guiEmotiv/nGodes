function setup() {
    createCanvas(600,600);
    // loadJSON("../json/client.json",data);
}
function draw() {
    c = color('hsba(160, 100%, 50%, 0.5)');
    background(c);
    fill(200,220,0);
    text("carlos",10,50);
}
